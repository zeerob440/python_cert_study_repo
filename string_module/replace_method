# studies the replace() method
# syntax
# targetString.replace('targetSequence', 'replacementSequence', iterationsOfReplacement)

rep_string: str = 'this is it'
# replaces first target sequence
print(rep_string.replace('is', '00', 1))
# replaces both instances
print(rep_string.replace('is', '00', 2))

