# lower() method
# makes a copy of source string, replaces all uppercase chars with lowercase chars
# upper() method does the opposite
# title() method, capitalizes each letter after a white space

upper_str: str = 'mk19 Automatic grenade Launcher' 
print(upper_str)

print(upper_str.upper(), upper_str.lower(), upper_str.title())